- name: install nginx server and run
  hosts: web
  tasks:
  - name: install nginx server
    ansible.builtin.package:
      msg: "installing nginx"
      name: nginx
      state: installed
  
  - name: run services
    ansible.builtin.service:
      msg: "enabling the services"
      name: nginx
      state: started
      enabled: yes

