- name: data types
  hosts: local
  connection: local
  vars:
    course: Devops
    duratioin: 120
    topics:
    - linux
    - shell
    - ansible
    live: true
    tools:
      cm: ansible
      script: shell
      cicd: jenkins
  tasks:
  - name: enter data types
    ansible.builtin.debug:
      msg: Hii, im learning {{ course }} and duration is {{ duration }}, topics are {{ topics }} with tools {{ tools }}
