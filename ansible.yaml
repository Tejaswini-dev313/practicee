- name: install nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx on servers
    ansible.builtin.package:
      name: nginx
      state: removed
  
  - name: enable the services
    ansible.builtin.service:
      name: nginx
      state: stopped
      enabled: no
