- name: install nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx on servers
    ansible.builtin.package:
      name: nginx
      state: installed
  
  - name: enable the services
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
      