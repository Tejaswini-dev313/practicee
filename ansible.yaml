- name: install nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx on servers
    ansible.builtin.package:
      name: nginx
      state: installed
  
  # - name: stop and disable services of nginx
  #   ansible.builtin.service:
  #     name: nginx
  #     state: stopped
  #     enabled: no
