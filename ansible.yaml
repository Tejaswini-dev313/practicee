- name: variables
  hosts: web
  vars:
    course: "Devops"
    duration: "120 hours"
    endtime: "july 30th"
  tasks:
  - name: typing the message using variables
    ansible.builtin.debug:
      msg: "I have restarted learing {{course}} and duration is {{duration}}. I have to finish this {{course}} by {{endtime}}"
  
  - name: typing the message again
    vars:
      course: AWS
    ansible.buildin.debug:
      msg: "I have started learing {{course}}"
